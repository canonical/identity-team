name: Release Charm Workflow

on:
  workflow_call:

jobs:
  quality-checks:
    name: Quality Checks
    uses: ./.github/workflows/_charm-quality-checks.yaml@main

  release-charm:
    name: Release Charm
    needs:
      - quality-checks
    uses: ./.github/workflows/_charm-release.yaml@main
    secrets: inherit
