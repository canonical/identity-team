name: Charm Release Workflow

on:
  workflow_call:

jobs:
  quality-checks:
    name: Quality Checks
    uses: ./.github/workflows/_charm-quality-checks.yaml

  release-charm:
    name: Release Charm
    needs:
      - quality-checks
    uses: ./.github/workflows/_charm-release.yaml
    secrets: inherit
